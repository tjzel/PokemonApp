import { StyleSheet, Text, View, Image, Pressable } from 'react-native';

export default function PokemonMapTab(){
  return(
    <View style={{width:200, heighy:200}}>
      <Text>Pokemon Map tab is empty...</Text>
    </View>
  )
}